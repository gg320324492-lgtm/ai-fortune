name: Aliyun 部署�?www.mnb-lab.cn

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  fate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Build static site (out/)
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm run build
          test -d out
          touch out/.nojekyll
          echo "sha=$GITHUB_SHA" > out/fate.txt
          echo "time=$(date -u)" >> out/fate.txt
          echo "target=aliyun" >> out/fate.txt

      - name: fate to Aliyun via rsync + reload nginx
        shell: bash
        env:
          SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
        run: |
          set -euo pipefail
          TARGET_HOST="60.205.93.8"
          TARGET_USER="fate"
          TARGET_PORT="22"
          REMOTE_DIR="/var/www/html"

          mkdir -p ~/.ssh
          printf '%s' "$SSH_PASSWORD" | tr -d '\r' > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p "$TARGET_PORT" "$TARGET_HOST" >> ~/.ssh/known_hosts

          sshpass -p "$SSH_PASSWORD" ssh -p "$TARGET_PORT" "$TARGET_USER@$TARGET_HOST" "mkdir -p '$REMOTE_DIR'"

          rsync -az --delete \
            -e "sshpass -p "$SSH_PASSWORD" ssh -p $TARGET_PORT -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes" \
            out/ "$TARGET_USER@$TARGET_HOST:$REMOTE_DIR/"

          sshpass -p "$SSH_PASSWORD" ssh -p "$TARGET_PORT" "$TARGET_USER@$TARGET_HOST" "sudo nginx -t && sudo systemctl reload nginx"
